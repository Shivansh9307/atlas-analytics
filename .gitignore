# secrets — never commit
.env
.env.local
*.pem
credentials*.json
token.json
*_token.json
.gslides_token.json

# python env
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# atlas run artefacts (regenerated; keep out of history)
runs/*
!runs/.gitkeep

# local data & engine files
data/*
!data/.gitkeep
*.duckdb
*.duckdb.wal

# runtime-accumulating knowledge stores (per-deployment; created on first write)
memory/query_archive.jsonl
memory/corrections.jsonl
memory/miss_rate.jsonl
memory/user_profile.json

# os
.DS_Store

# Data Quality Copilot: runtime semantic-clean manifests (regenerated on connect)
clean_layers/
